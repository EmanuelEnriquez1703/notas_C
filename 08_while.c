#include <stdio.h>

int main(int argc, char const *argv[])
{
    int contador = 0;

    while (contador <= 10)
    {
        printf("numeros del ciclo: %i\n",contador);
        contador++;
    }
    
    return 0;
}
